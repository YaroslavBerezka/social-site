import React from "react"

const Settings: React.FC<IProps> = React.memo((props) => {
    return (
        <div>
            Settings
        </div>
    );
});

export default Settings;

interface IProps {

};